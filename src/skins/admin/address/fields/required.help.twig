{##
 # Required text
 #}

{% if this.getHelpText(this.column, this.entity) %}
  {{ widget('\\XLite\\View\\Tooltip',
    id='menu-links-help-text',
    text=this.getHelpText(this.column, this.entity),
    isImageTag='true',
    className='help-small-icon') }}
{% endif %}
