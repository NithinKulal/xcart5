{##
 # Just a template for settings. We need it since we do not implement decorator for modules now
 #}

{{ widget('\\XLite\\View\\FormField\\Select\\FloatFormat', fieldOnly='1', value=this.getColumnValue(this.column, this.entity), fieldName=this.getFormatSelectorName(this.column, this.entity), fieldId=this.getFormatSelectorId(this.column, this.entity)) }}