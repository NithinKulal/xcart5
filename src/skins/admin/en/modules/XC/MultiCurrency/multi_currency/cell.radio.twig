{##
 # Just a template for settings. We need it since we do not implement decorator for modules now
 #}

{{ widget('\\XLite\\View\\FormField\\Input\\Radio', fieldOnly='1', fieldName='default_currency', value=this.getActiveCurrencyId(this.entity), isChecked=this.isDefaultCurrency(this.entity)) }}