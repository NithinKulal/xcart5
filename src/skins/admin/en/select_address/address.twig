{##
 # Plain address block
 #}

<div class="address-box-content clearfix">
  <ul class="address-box">
    {% for fieldName, fieldData in this.getAddressFields() %}
      {% if this.getFieldValue(fieldName, this.address) %}
        <li class="address-text-cell address-text-{{ fieldName }}" data-name="{{ fieldName }}">
          <ul class="address-text">
            <li class="address-text-label address-text-label-{{ fieldName }}">{{ fieldData.label }}:</li>
            <li class="address-text-value">{{ this.getFieldValue(fieldName, this.address) }}</li>
            <li class="address-text-comma address-text-comma-{{ fieldName }}">,</li>
          </ul>
        </li>
      {% endif %}
    {% endfor %}
  </ul>
</div>
