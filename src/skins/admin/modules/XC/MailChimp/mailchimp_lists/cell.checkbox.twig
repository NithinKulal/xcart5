{##
 # Just a template for settings. We need it since we do not implement decorator for modules now
 #}
{{ widget('\\XLite\\View\\FormField\\Input\\Checkbox', fieldOnly='1', fieldName=this.getCheckBoxName(this.entity), fieldId=this.getCheckBoxId(this.entity), value='1', isChecked=this.isDefaultList(this.entity)) }}