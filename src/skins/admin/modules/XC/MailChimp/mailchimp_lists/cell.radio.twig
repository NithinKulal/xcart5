{##
 # Just a template for settings. We need it since we do not implement decorator for modules now
 #}
{{ widget('\\XLite\\View\\FormField\\Input\\Radio', fieldOnly='1', fieldName=this.getRadioName(), fieldId=this.getRadioId(this.entity), value=this.getListId(this.entity), isChecked=this.isDefaultList(this.entity)) }}